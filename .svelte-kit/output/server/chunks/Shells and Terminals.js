import { G as sanitize_props, J as spread_props } from "./index.js";
import { M as MarkdownLayout } from "./MarkdownLayout.js";
const metadata = {
  "title": "Learn Shells and Terminals",
  "tags": ["boot-dev", "basic-programming", "shell-utilities"],
  "date": "2/1/25"
};
const { title, tags, date } = metadata;
function Shells_and_Terminals_md($$renderer, $$props) {
  const $$sanitized_props = sanitize_props($$props);
  MarkdownLayout($$renderer, spread_props([
    $$sanitized_props,
    metadata,
    {
      children: ($$renderer2) => {
        $$renderer2.push(`<h3>CLI:</h3> <ul><li>You’ll often hear the terms “terminal”, “shell”, “command line”, “CLI”, and “command prompt” used interchangeably, but often they all refer to the same thing: a program that allows you to interact with your computer in a text-based way.</li></ul> <h3>What’s a GUI?</h3> <p>If you don’t have a technical background, you’re probably used to interacting with your phone or computer using a <a href="https://en.wikipedia.org/wiki/Graphical_user_interface" rel="nofollow">graphical user interface</a> (GUI). When you use a mouse to click on fancy icons, buttons, and menus, you’re using a GUI.</p> <p>To be fair, it’s usually easier to teach someone how to use a GUI than a CLI. You can simply point to things and say “click here” or “drag this there”. But GUIs do have some drawbacks:</p> <ul><li><strong>They’re weak.</strong> You are given much more control over your computer through a CLI. With a GUI you’re limited to the options that the developer of the GUI has given you.</li> <li><strong>They’re slow.</strong> Once you know the commands to type, it’s much faster to type them than to click through endless menus with a mouse.</li> <li><strong>They’re not as reproducible.</strong> If you want to share a set of instructions, you can just copy and paste commands without worrying about screen sizes and user preferences.</li> <li><strong>They’re not automatable.</strong> It’s easy to write code that manipulates text (as you’ve seen in Python), but it’s much harder to write code that manipulates GUIs.</li> <li><strong>They’re not as cool.</strong> You will be invited to 90% fewer romantic outings if you are a GUI user.</li></ul> <h3>What is a terminal?</h3> <ul><li>As we talked about, the terms “shell”, “CLI”, and “terminal” are often used to refer to the same thing: the program that lets you issue text-based commands.</li> <li>However, to get pedantic, the “terminal” is just one <em>specific part</em> of that program. Historically, the word “terminal” meant a physical device that you could type commands into, essentially a keyboard and a screen.</li> <li>A terminal emulator is a program that emulates a physical terminal. It’s a program that lets you type commands into a window on your computer.</li> <li><em>Which</em> commands you’re able to use isn’t determined by the terminal emulator that you happen to be using. It’s determined by the “shell”, which we’ll talk about later.</li></ul> <h3>What is a shell?</h3> <ul><li>So if your terminal is just a program that lets you issue text-based commands and renders the output of those commands…
…What is the program that <em>runs</em> those commands???</li> <li>That’s a shell.</li> <li>Shells do a lot of things, but their main job is to interpret the commands you type and execute them.</li> <li>Shells are often referred to as “REPL”s. REPL stands for <ul><li>Read</li> <li>Eval (evaluate)</li> <li>Print</li> <li>Loop</li></ul></li> <li>This is a fancy way of saying that shells are programs that: <ol><li>Read the commands you type</li> <li>Evaluate those commands, usually by running other programs on your computer</li> <li>Print the output of those commands</li> <li>Give you a new prompt to type another command and repeat</li></ol></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94"># to eval a result</span></span>
<span class="line"><span style="color:#FFC799">expr</span><span style="color:#FFC799"> 123456</span><span style="color:#99FFE4"> +</span><span style="color:#FFC799"> 7890</span></span></code></pre> <h3>Variables:</h3> <ul><li>Both Bash and Zsh are shells, and they also happen to be powerful programming languages. They have variables, functions, loops, and more. That said, only <a href="https://bashsta.cc/" rel="nofollow">crazy people</a> write large programs in shell languages… shells are optimized for running other programs and writing small scripts, not for writing programs.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFF">name</span><span style="color:#A0A0A0">=</span><span style="color:#99FFE4">"hello"</span></span></code></pre> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFF">bankname</span><span style="color:#A0A0A0">=</span><span style="color:#99FFE4">"WorldBanc"</span></span>
<span class="line"><span style="color:#FFF">founded</span><span style="color:#A0A0A0">=</span><span style="color:#99FFE4">"1969"</span></span>
<span class="line"><span style="color:#FFF">ceo</span><span style="color:#A0A0A0">=</span><span style="color:#99FFE4">"Jeff Gates"</span></span>
<span class="line"><span style="color:#FFC799">echo</span><span style="color:#99FFE4"> "</span><span style="color:#FFF">$bankname</span><span style="color:#99FFE4"> was founded in </span><span style="color:#FFF">$founded</span><span style="color:#99FFE4"> by </span><span style="color:#FFF">$ceo</span><span style="color:#99FFE4">"</span></span></code></pre> <h3>History:</h3> <ul><li>hen you’re working in a REPL, it’s <em>really</em> helpful to be able to see the commands you’ve typed in the past. That way you can easily re-run them, or copy and paste them into a script.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">history</span></span></code></pre> <ul><li>You’ll often want to re-run a command that you’ve run before. You could just type it out again, but assuming you don’t have the <a href="https://en.wikipedia.org/wiki/Words_per_minute" rel="nofollow">WPM</a> of ThePrimeagen, that’s going to be a pain.</li> <li>Instead, you can use the up and down arrows to cycle through your command history. Focus your terminal window and use the “up” arrow key to start cycling through your command history. If you recently restarted your terminal type a few commands first, like:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">echo</span><span style="color:#99FFE4"> hello</span></span>
<span class="line"><span style="color:#FFC799">echo</span><span style="color:#99FFE4"> world</span></span></code></pre> <ul><li>If your terminal is feeling cluttered with text, you can clear it with the <code>clear</code> command, or by pressing <code>ctrl + l</code>.</li></ul> <h3>FileSystems:</h3> <ul><li>All the data stored on your computer is organized into files and directories. Files and directories are organized into a tree-like structure called a filesystem.</li> <li>Directories (same as “folders” on Windows) are just containers that hold files and other directories.</li> <li>Files are just a dump of raw binary data: 1’s and 0’s. The bytes in a file can represent anything: text, images, videos, etc.</li> <li>The filesystem tree starts with a single directory called the <a href="https://en.wikipedia.org/wiki/Root_directory" rel="nofollow">root directory</a>. The root directory contains files and directories, which can contain more files and directories, and so on.</li> <li>When you open your terminal, your <em>working directory</em> (the one you’re “in”) is going to be… somewhere. Most commonly it is your <a href="https://en.wikipedia.org/wiki/Home_directory" rel="nofollow">“home” directory</a>.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94"># get the working dir</span></span>
<span class="line"><span style="color:#FFC799">pwd</span></span></code></pre> <ul><li>The output of your <code>pwd</code> command is a <em>filepath</em>. A filepath is a string that describes the location of a file or directory on your computer. Yours should look something like this:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span>/Users/wagslane</span></span></code></pre> <ul><li>The text might be different, but the structure should be the same. Let’s break it down: <ul><li>The first slash (<code>/</code>) represents the <em>root directory</em>. It’s the tippy-top of the filesystem tree.</li> <li>The next part (<code>Users</code>) is the name of a directory inside the root directory.</li> <li>Finally, the last part (<code>wagslane</code>) is the name of a directory inside the <code>Users</code> directory.</li></ul></li> <li>So this path represents a directory 2 levels down from the root directory:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span>root</span></span>
<span class="line"><span>  └── Users</span></span>
<span class="line"><span>        └── wagslane</span></span></code></pre> <h3>Parent Directories:</h3> <ul><li> how you can “change directory” to move <em>into</em> a directory. But how do you move back <em>out</em> of a directory?</li> <li>The answer is two dots: <code>..</code></li> <li><code>..</code> is a special directory name that means “the parent directory”. It’s a shortcut that you can use to move up one level in the directory tree.</li></ul> <h3>Absolute vs Relative Paths:</h3> <ul><li>We’ve mostly been dealing with <a href="https://www.redhat.com/sysadmin/linux-path-absolute-relative" rel="nofollow">relative filepaths</a> which are just paths that take into account your current directory.</li> <li>For example, let’s say we have the following directory structure in our filesystem:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span>vehicles</span></span>
<span class="line"><span>├── cars</span></span>
<span class="line"><span>│   ├── fords</span></span>
<span class="line"><span>│   │   ├── mustang.txt</span></span>
<span class="line"><span>│   │   └── focus.txt</span></span></code></pre> <ul><li>When inside the top-level <code>vehicles</code> directory, the relative path to the <code>mustang.txt</code> file is:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span>cars/fords/mustang.txt</span></span></code></pre> <ul><li>However, when we’re inside the <code>cars</code> directory, the relative path to the <code>mustang.txt</code> file is just:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span>fords/mustang.txt</span></span></code></pre> <ul><li>Or when inside the <code>fords</code> directory, just:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span>mustang.txt</span></span></code></pre> <ul><li>An absolute path is a path that starts at the root of the filesystem. On <a href="https://en.wikipedia.org/wiki/Unix-like" rel="nofollow">Unix-like systems</a> (Mac OS/Linux/WSL), the root is denoted by a forward slash <code>/</code>. So, if the <code>vehicles</code> directory is in the filesystem root, the absolute path to the <code>mustang.txt</code> file is</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span>/vehicles/cars/fords/mustang.txt</span></span></code></pre> <h3>Files:</h3> <ul><li>At their core, files are just blobs of data. The raw bytes in a file can represent anything: text, images, videos, etc.</li> <li><code>cat</code> command is used to view the contents of a file. It’s short for “concatenate”, which is a fancy way of saying “put things together”. It can feel like a confusing name if you’re using the <code>cat</code> command to view a single file, but it makes more sense when you’re using it to view multiple files at once.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94"># Print the contents of a file to the terminal</span></span>
<span class="line"><span style="color:#FFC799">cat</span><span style="color:#99FFE4"> file1.txt</span></span></code></pre> <ul><li>Sometimes you don’t want to print <em>everything</em> in a file. Files can be really big after all.</li> <li>The head command <ul><li>The <a href="https://www.ibm.com/docs/en/aix/7.3?topic=h-head-command" rel="nofollow">head</a> command prints the first <code>n</code> lines of a file, where <code>n</code> is a number you specify.</li></ul></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">head</span><span style="color:#99FFE4"> -n</span><span style="color:#FFC799"> 10</span><span style="color:#99FFE4"> file1.txt</span></span></code></pre> <ul><li>The tail command <ul><li>The <a href="https://www.ibm.com/docs/en/aix/7.3?topic=t-tail-command" rel="nofollow">tail</a> command prints the <em>last</em> <code>n</code> lines of a file, where <code>n</code> is a number you specify.</li></ul></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">tail</span><span style="color:#99FFE4"> -n</span><span style="color:#FFC799"> 10</span><span style="color:#99FFE4"> file1.txt</span></span></code></pre> <ul><li>The <a href="https://www.ibm.com/docs/en/aix/7.3?topic=m-more-command" rel="nofollow">more</a> and <a href="https://man7.org/linux/man-pages/man1/less.1.html" rel="nofollow">less</a> commands let you view the contents of a file, one page (or line) at a time.</li> <li>As the adage goes, <code>less</code> is <code>more</code>.</li> <li>In the context of these commands, <code>less</code> is <em>literally</em> <code>more</code>. The <code>less</code> command does everything that the <code>more</code> command does but also has more features. As a general rule, you should use <code>less</code> instead of <code>more</code>.</li> <li>You would only use <code>more</code> if you’re on a system that doesn’t have <code>less</code> installed.</li> <li><code>less</code> command, but this time, pass in the <code>-N</code> flag to show line numbers:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">less</span><span style="color:#99FFE4"> -N</span><span style="color:#99FFE4"> 2023.csv</span></span></code></pre> <ul><li>The <a href="https://man7.org/linux/man-pages/man1/touch.1.html" rel="nofollow">touch command</a> is designed to update the access and modification timestamps of a file. By default, if the specified file does not exist, <code>touch</code> will create an empty file with the given filename. Because of this, you’ll often see this command used to quickly create new files.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">touch</span><span style="color:#99FFE4"> new_file.txt</span></span></code></pre> <ul><li>You can also create multiple files at once by listing them:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">touch</span><span style="color:#99FFE4"> some_file.txt</span><span style="color:#99FFE4"> some_other_file.txt</span></span></code></pre> <h3>Directories:</h3> <ul><li>a directory is just a location in a filesystem that can contain files and other directories. On some systems, directories are called “folders”, but it’s the same thing.</li> <li>The <a href="https://www.ibm.com/docs/en/aix/7.3?topic=m-mkdir-command" rel="nofollow">“make directory” command</a> creates a new directory inside the current directory.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">mkdir</span><span style="color:#99FFE4"> my_directory</span></span></code></pre> <h3>Move:</h3> <ul><li>The <a href="https://www.ibm.com/docs/en/aix/7.3?topic=files-moving-renaming-mv-command" rel="nofollow">move command</a> moves a file or directory from one location to another. You can use it to rename a file or to move it to a different directory altogether. Your working directory can’t be the directory you’re moving.</li> <li>Renaming a file:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">mv</span><span style="color:#99FFE4"> some_file.txt</span><span style="color:#99FFE4"> some_other_name.txt</span></span></code></pre> <ul><li>Moving a file from the current directory to another nested directory:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">mv</span><span style="color:#99FFE4"> some_file.txt</span><span style="color:#99FFE4"> some_directory/some_file.txt</span></span></code></pre> <ul><li>Moving a file from the current directory, to the parent directory:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">mv</span><span style="color:#99FFE4"> some_file.txt</span><span style="color:#99FFE4"> ../some_file.txt</span></span></code></pre> <ul><li>If you don’t want to rename the file and you’re just moving it to a different directory, you can omit the filename:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">mv</span><span style="color:#99FFE4"> some_file.txt</span><span style="color:#99FFE4"> some_directory/</span></span></code></pre> <h3>Remove:</h3> <ul><li>The <a href="https://www.ibm.com/docs/en/aix/7.3?topic=files-deleting-rm-command" rel="nofollow">remove command</a> deletes a file or empty directory:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">rm</span><span style="color:#99FFE4"> some_file.txt</span></span></code></pre> <ul><li>You can optionally add a <code>-r</code> flag to tell the <code>rm</code> command to delete a directory and <em>all</em> of its contents recursively. “Recursively” is just a fancy way of saying “do it again on all of the subdirectories and their contents”.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">rm</span><span style="color:#99FFE4"> -r</span><span style="color:#99FFE4"> some_directory</span></span></code></pre> <h3>Copy:</h3> <ul><li><a href="https://www.ibm.com/docs/en/aix/7.3?topic=c-cp-command" rel="nofollow">copy command</a> does what you would (hopefully) expect: it copies a file from one location to another.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">cp</span><span style="color:#99FFE4"> source_file.txt</span><span style="color:#99FFE4"> destination/</span></span></code></pre> <ul><li>You can also copy a directory and all of its contents recursively by adding the <code>-R</code> flag:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">cp</span><span style="color:#99FFE4"> -R</span><span style="color:#99FFE4"> my_dir</span><span style="color:#99FFE4"> new_dir</span></span></code></pre> <h3>Home:</h3> <ul><li><p>In a Unix-like operating system, a user’s <a href="https://en.wikipedia.org/wiki/Home_directory" rel="nofollow">home directory</a> is the directory where their personal files are stored. It is also the directory that a user starts in when logging into the system.</p></li> <li><p>I recommend doing all of your development work in your home directory. For example, I like to create a <code>workspace</code> directory in my home directory, and all my projects live in subdirectories there.</p></li> <li><p>Your home directory is where you want to spend most of your time. Many of the other directories on your machine are critical to the operating system or other programs. Be careful when working in <a href="https://www.ibm.com/docs/en/aix/7.3?topic=reference-directories" rel="nofollow">other directories</a> like <code>/bin</code>, <code>/etc</code>, <code>/var</code>, etc.</p></li> <li><p>You can mess up your system if you’re not careful.</p></li> <li><p>The <code>~</code> alias</p> <ul><li>My home directory (on Mac) is located at <code>/Users/wagslane</code>. The <code>~</code> character is an alias for your home directory. So when I want to go home, I don’t have to type out <code>cd /Users/wagslane</code>, I can just type:</li></ul></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">cd</span><span style="color:#99FFE4"> ~</span></span></code></pre> <h3>Grep:</h3> <ul><li>You might be used to nice graphical interfaces that allow you to search for text in files, usually with <code>ctrl+f</code> or <code>cmd+f</code>. But what about when you’re working on a terminal?</li> <li>As it turns out, once you’re used to it, searching for text in files on a CLI can be much faster than using a GUI.</li> <li>The <a href="https://www.digitalocean.com/community/tutorials/grep-command-in-linux-unix" rel="nofollow">grep command</a> allows you to search for text in files. It has a <em>ton</em> of capability, and we’ll only be scratching the surface of its true power.</li> <li>Basic usage</li> <li>The most basic usage of <code>grep</code> is to search for a string in a file. For example, if we wanted to search for the word “hello” in the file <code>hello.txt</code>, we could run:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">grep</span><span style="color:#99FFE4"> "hello"</span><span style="color:#99FFE4"> hello.txt</span></span></code></pre> <ul><li>You can also search multiple files at once. For example, if we wanted to search for the word “hello” in <code>hello.txt</code> and <code>hello2.txt</code>, we could run:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">grep</span><span style="color:#99FFE4"> "hello"</span><span style="color:#99FFE4"> hello.txt</span><span style="color:#99FFE4"> hello2.txt</span></span></code></pre> <ul><li>Recursive search</li> <li>You can also search an entire directory, including all subdirectories. For example, if we wanted to search for the word “hello” in the current directory and all subdirectories, we could run:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94"># . is the current directory</span></span>
<span class="line"><span style="color:#FFC799">grep</span><span style="color:#99FFE4"> -r</span><span style="color:#99FFE4"> "hello"</span><span style="color:#99FFE4"> .</span></span></code></pre> <h3>Find:</h3> <ul><li>The <code>find</code> command is a powerful tool for finding files and directories by name, not by their contents.</li> <li>Find a file by name <ul><li>Let’s say you’re looking for a file named <code>hello.txt</code> somewhere in your home directory. You can use the <code>find</code> command to search for exactly that title:</li></ul></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">find</span><span style="color:#99FFE4"> some_directory</span><span style="color:#99FFE4"> -name</span><span style="color:#99FFE4"> hello.txt</span></span></code></pre> <ul><li>Pattern search <ul><li>The <code>find</code> command can also search for files that match a pattern. For example, if you wanted to find all files that end in <code>.txt</code>, you could run:</li></ul></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">find</span><span style="color:#99FFE4"> some_directory</span><span style="color:#99FFE4"> -name</span><span style="color:#99FFE4"> "*.txt"</span></span></code></pre> <ul><li>The <code>*</code> character is a wildcard that matches anything. If you’re trying to find filenames that <em>contain</em> a specific word, you can use the <code>*</code> character to match the rest of the filename:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94"># Find all filenames that contain the word "chad"</span></span>
<span class="line"><span style="color:#FFC799">find</span><span style="color:#99FFE4"> some_directory</span><span style="color:#99FFE4"> -name</span><span style="color:#99FFE4"> "*chad*"</span></span></code></pre> <h3>Permissions:</h3> <ul><li>Unix-like systems (like the one you’re using) support multiple users. Each user has their own home directory, their own files, and their own permissions.</li> <li>If you’re like most people these days, you’re the only user on your machine. It used to be more common for multiple people to share a single computer, or for multiple people to do their work on the same computer over a network.</li> <li><code>sudo</code> keyword lets you run a command as a “superuser”. It’s short for <a href="https://www.linux.com/training-tutorials/linux-101-introduction-sudo/" rel="nofollow">“superuser do”</a>. To use it, you’ll need a password with superuser privileges, which you should already have if you’re the only user of your machine</li> <li>Some people are pedantic and pronounce <code>sudo</code> as “sue-doo”. Others are correct and pronounce it “sue-dough”.</li> <li>First, run the <a href="https://www.ibm.com/docs/en/zos/3.1.0?topic=wtsc-using-whoami-command" rel="nofollow">“Who am I?” command</a> to see which user you’re logged in as:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">whoami</span></span></code></pre> <ul><li>In a Unix-like operating system, permissions control who can do what to which files and directories. The permissions of an individual file or directory are visually represented as a 10-character string:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span>drwxrwxrwx</span></span></code></pre> <ul><li><p>Let’s break down each character. The first one just tells you whether you’re looking at a file or a directory:</p> <ul><li><code>-</code>: Regular file (e.g. <code>-rwxrwxrwx</code>)</li> <li><code>d</code>: Directory (e.g. <code>drwxrwxrwx</code>)</li></ul></li> <li><p>The next 9 characters are broken up into 3 sets of <code>rwx</code> and represent the permissions themselves for the “owner”, “group”, and “others”, in order. Each group of 3 represents the permissions for reading, writing, and executing, in order. So, for example:</p> <ul><li><code>rwx</code>: All permissions</li> <li><code>rw-</code>: Read and write, but not execute</li> <li><code>r-x</code>: Read and execute, but not write</li> <li>The first 3 characters are “owner” permissions. The “owner” is usually just the user who created the file or directory, but it can be manually changed.</li> <li>The next 3 characters are “group” permissions. Unix-like systems support groups of users and a file or directory can be assigned to a single group. To be honest, unless you’re a system administrator, you won’t often worry about groups.</li> <li>The last 3 characters are “others” permissions. This is everyone else.</li></ul></li> <li><p>In my experience, when you’re doing programming work on your own local machine, you mostly just care about the “owner” permissions because that’s usually you. Here are some full examples:</p> <ul><li><code>-rwxrwxrwx</code>: A file where everyone can do everything</li> <li><code>-rwxr-xr-x</code>: A file where everyone can read and execute, but only the owner can write</li> <li><code>drwxr-xr-x</code>: A directory where everyone can read (<code>ls</code> the contents) and execute (<code>cd</code> into it), but only the owner can write (modify the contents)</li> <li><code>drwx------</code>: A directory where only the owner can read, write and execute</li></ul></li> <li><p>The <a href="https://www.ibm.com/docs/en/aix/7.3?topic=c-chmod-command" rel="nofollow">chmod command</a> lets you change the permissions of a file or directory. It’s short for “change mode” (I wish it was called “change permissions”, but alas).</p></li> <li><p>The <code>ls</code> command has a <code>-l</code> option (lowercase “L”) that will print out the permissions of each file and directory in long format. Run this command inside the <code>worldbanc/private</code> directory:</p></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">ls</span><span style="color:#99FFE4"> -l</span></span></code></pre> <ul><li>Change the permissions of the <code>private</code> directory and all of its contents so that: <ul><li>The owner can read, write, and execute</li> <li>The group can do nothing</li> <li>Others can do nothing</li></ul></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">chmod</span><span style="color:#99FFE4"> -R</span><span style="color:#99FFE4"> u=rwx,g=,o=</span><span style="color:#99FFE4"> DIRECTORY</span></span></code></pre> <ul><li>In the command above, <code>u</code> means “user” (aka “owner”), <code>g</code> means “group”, and <code>o</code> means “others”. The <code>=</code> means “set the permissions to the following”, and the <code>rwx</code> means “read, write and execute”. The <code>g=</code> and <code>o=</code> mean “set group and other permissions to nothing”. The <code>-R</code> means “recursively”, which means “do this to all of the contents of the directory as well”.</li> <li>Executables <ul><li>You’re probably familiar with the idea of reading and writing files. But what about executing them? Executable files are just programs that you can run on your computer.</li> <li>Files with a <code>.sh</code> extension are <a href="https://en.wikipedia.org/wiki/Shell_script" rel="nofollow">shell scripts</a>. They’re just text files that contain shell commands. You can run a file in your shell by just typing its filepath.</li> <li>Interestingly, if the program is in the current directory (in this example, the <code>mydir</code> directory), you need to prefix it with <code>./</code>:</li></ul></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">./program.sh</span></span></code></pre> <ul><li>can add <code>chmod -x &lt;script-name></code> to remove permissions and <code>+x</code> to ad back those permissions.</li></ul> <h3>Root User:</h3> <ul><li>“root” user is a superuser. It has access to everything on the system and can do anything. When you use the <code>sudo</code> command, you’re running as the root user (as long as your system hasn’t been configured differently).</li> <li>The <code>sudo</code> keyword is convenient because it quickly gives you elevated permissions to run a single command.</li> <li>It can also be dangerous because it gives you access to everything. If you run a command with <code>sudo</code> that you don’t understand, you could do serious damage to your system.</li> <li>For example, <code>rm</code> with the <code>r</code> and <code>f</code> flags run on the root directory (<code>/</code>), will <strong>delete all the files on your system</strong>. Don’t do that. The <code>r</code> flag is for “recursive” (delete everything inside) and the <code>f</code> flag is for “force”. Most systems will prevent you from doing this, but if you run it with <code>sudo</code>, you’ve just turned your computer into a very expensive paperweight.</li> <li>So when do you <em>need</em> to use <code>sudo</code>? <code>chmod</code> allows you to change the permissions of any file or directory that you own. But what if you don’t own the file or directory? That’s where <code>sudo</code> is required. Let’s change ownership of a directory to see how that works.</li> <li>The <code>chown</code> command, which stands for “change owner”, allows you to change the owner of a file or directory, and it requires root privileges.</li></ul> <h3>Compiled vs Interpreted Programs:</h3> <ul><li>A program is just a set of instructions that a computer can execute. We talked about executables in the last lesson, an executable is just a file that contains a program. The words “program” and “executable” are often used interchangeably. Broadly speaking, there are two types of programs: <ul><li>Compiled programs <ul><li>A compiled program is a program that has been converted from human-readable source code into machine code (binary). <a href="https://en.wikipedia.org/wiki/Machine_code" rel="nofollow">Machine code</a> is a set of instructions that a computer can execute directly: your computer’s CPU is hardware that’s been designed to execute machine code.</li> <li>Programming languages like Go, C, and Rust are compiled languages that produce compiled programs.</li></ul></li> <li>Interpreted programs: <ul><li>An interpreted program is a program that is executed by <em>another</em> program. The program that executes the interpreted program is called an <a href="https://en.wikipedia.org/wiki/Interpreter_%28computing%29" rel="nofollow">interpreter</a>. The interpreter reads the source code of the interpreted program and executes it.</li> <li>Programming languages like Python, Ruby, and JavaScript, are interpreted languages. Your computer needs to have an interpreter installed to run programs written in those languages.</li></ul></li></ul></li></ul> <h3>Shebang:</h3> <ul><li>That works out-of-the-box for files that are compiled executables. But what about scripts that need to be interpreted by another program? The computer needs to be told what program to use to interpret the file.</li> <li>A <a href="https://en.wikipedia.org/wiki/Shebang_(Unix)" rel="nofollow">“shebang”</a> is a special line at the top of a script that tells your shell which program to use to execute the file.</li> <li>The format of a shebang is:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94">#! interpreter [optional-arg]</span></span></code></pre> <ul><li>For example, if your script is a Python script and you want to use Python 3, your shebang might look like this:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94">#!/usr/bin/python3</span></span></code></pre> <ul><li>For your main shell REPL, as we talked about before:</li> <li>If you’re using Ubuntu on WSL, you’re probably running a <a href="https://en.wikipedia.org/wiki/Bash_(Unix_shell)" rel="nofollow">Bash</a> shell.</li> <li>If you’re using macOS, you’re probably running a <a href="https://en.wikipedia.org/wiki/Z_shell" rel="nofollow">Zsh</a> shell.</li> <li>If you’re running a raw Linux installation, I pray you already know what you’re using.</li> <li>To get hand-wavy about it, I want to explain the difference between the 3 shells you’re likely to encounter: <ul><li><code>sh</code> - The Bourne shell. This is the original Unix shell and is <a href="https://en.wikipedia.org/wiki/POSIX" rel="nofollow">POSIX-compliant</a>. It’s very basic and doesn’t have many quality-of-life features.</li> <li><code>bash</code> - The Bourne Again shell. This is the most popular shell on Linux. It builds on <code>sh</code>, but also has a lot of extra features.</li> <li><code>zsh</code> - The Z shell. This is the most popular shell on macOS. Like <code>bash</code>, it does what <code>sh</code> can do, but also has a lot of extra features.</li></ul></li> <li>Both <code>zsh</code> and <code>bash</code> are “sh-compatible” shells, meaning they can run <code>.sh</code> scripts, but they also have extra features that generally make them more pleasant to use. For your purposes, the differences between <code>zsh</code> and <code>bash</code> are not super significant.</li></ul> <h3>Shell Configuration:</h3> <ul><li>Bash and Zsh both have <a href="https://en.wikipedia.org/wiki/Unix_shell#Configuration_files" rel="nofollow">configuration files</a> that run automatically each time you start a new shell session. These files are used to set up your shell environment. They can be used to set up aliases, functions, and environment variables.</li> <li>These files are located in your home directory (<code>~</code>) and are hidden by default. The <code>ls</code> command has a <code>-a</code> flag that will show hidden files:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">ls</span><span style="color:#99FFE4"> -a</span><span style="color:#99FFE4"> ~</span></span></code></pre> <h3>Environment Variables:</h3> <ul><li>We talked about how you can create and use local variables in your shell:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFF">name</span><span style="color:#A0A0A0">=</span><span style="color:#99FFE4">"Lane"</span></span>
<span class="line"><span style="color:#FFC799">echo</span><span style="color:#FFF"> $name</span></span>
<span class="line"><span style="color:#8B8B8B94"># Lane</span></span></code></pre> <ul><li>There is another type of variable called <a href="https://en.wikipedia.org/wiki/Environment_variable" rel="nofollow">environment variables</a>. They are available to <em>all</em> programs that you run in your shell.</li> <li>You can view all of the environment variables that are currently set in your shell with the command <code>env</code>.</li> <li>If you want to set a variable in your shell, you can use the <code>export</code> command:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#A0A0A0">export</span><span style="color:#FFF"> NAME</span><span style="color:#A0A0A0">=</span><span style="color:#99FFE4">"Lane"</span></span></code></pre> <ul><li>You can then use the variable in your shell, just as before:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">echo</span><span style="color:#FFF"> $NAME</span></span>
<span class="line"><span style="color:#8B8B8B94"># Lane</span></span></code></pre> <ul><li>The interesting part is that programs and scripts you run in your shell can <em>also</em> use that variable:</li> <li>For example, we can create a file called <code>introduce.sh</code> with the following contents:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94">#!/bin/sh</span></span>
<span class="line"><span style="color:#FFC799">echo</span><span style="color:#99FFE4"> "Hi I'm </span><span style="color:#FFF">$NAME</span><span style="color:#99FFE4">"</span></span></code></pre> <ul><li>Then we run it:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">chmod</span><span style="color:#99FFE4"> +x</span><span style="color:#99FFE4"> ./introduce.sh</span></span>
<span class="line"></span>
<span class="line"><span style="color:#FFC799">./introduce.sh</span></span>
<span class="line"><span style="color:#8B8B8B94"># Hi I'm Lane</span></span></code></pre> <h3>Path:</h3> <ul><li>There are environment variables that are sort of “built-in” to your shell. By “built-in” I just mean that different programs and parts of your system know about them and use them. The <code>PATH</code> variable is one of those.</li> <li>Why do we care ? <ul><li>If it weren’t for the <code>PATH</code>, you’d have to remember the filesystem path of every executable you wanted to run. Instead of just running <code>ls</code>, you’d have to run <code>/bin/ls</code> (or whatever the location of the <code>ls</code> executable is on your system). That’s not very convenient.</li> <li>The <code>PATH</code> variable is a list of directories that your shell will look into when you try to run a command. If you type <code>ls</code>, your shell will look in each directory listed in your <code>PATH</code> variable for an executable called <code>ls</code>. If it finds one, it will just run it. If it doesn’t, it will give you an error like: “command not found”.</li></ul></li></ul> <h3>Change Your Path:</h3> <ul><li>A common problem you’ll run into in the future is that you install a new program on your machine, but when you try to run it from your terminal, you get an error like:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">$</span><span style="color:#99FFE4"> my-new-program</span></span>
<span class="line"><span style="color:#FFC799">-bash:</span><span style="color:#99FFE4"> my-new-program:</span><span style="color:#99FFE4"> command</span><span style="color:#99FFE4"> not</span><span style="color:#99FFE4"> found</span></span></code></pre> <ul><li>Nine times out of ten, this is because the program is installed in a location that’s not in your <code>PATH</code> variable. Oftentimes when you install a program using the CLI, it will print a message during the installation process that tells you where the command was installed. <strong>Don’t let your eyes glaze over when your terminal prints important messages!</strong> Sometimes you just gotta <a href="https://www.dictionary.com/browse/rtfm" rel="nofollow">rtfm</a>.</li> <li>To add a directory to your <code>PATH</code> without overwriting all of the existing directories, use the <code>export</code> command and reference the existing <code>PATH</code> variable:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#A0A0A0">export</span><span style="color:#FFF"> PATH</span><span style="color:#A0A0A0">=</span><span style="color:#99FFE4">"</span><span style="color:#FFF">$PATH</span><span style="color:#99FFE4">:/path/to/new"</span></span></code></pre> <ul><li>The <code>$PATH</code> part is a reference to the existing <code>PATH</code> variable. The <code>:</code> separates the existing directories from the new directory (<code>/path/to/new</code>) that you’re adding.</li> <li>In the last lesson, you changed your <code>PATH</code> variable for your current shell session. Trouble is, the next time you restart your shell, it will be reset to its default value. You won’t be able to use the <code>worldbanc</code> CLI Tool from anywhere unless you change your <code>PATH</code> variable <em>permanently</em>.</li> <li>The most common way to do this is to add the same <code>export</code> command that you used in the last lesson to your shell’s configuration file.</li></ul> <h3>Man:</h3> <ul><li>The <a href="https://www.ibm.com/docs/en/aix/7.3?topic=m-man-command" rel="nofollow">man</a> command is short for “manual”. It’s a program that displays the manual for other programs.</li> <li>The <code>man</code> command will only work for programs that it has a manual for, but most built-in commands and Unix programs are supported. You just pass the name of the command you want to read the manual of as an argument. The most intuitive place to start, of course, is reading the manual-manual:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94"># open the man pages for the 'man' command</span></span>
<span class="line"><span style="color:#FFC799">man</span><span style="color:#99FFE4"> man</span></span></code></pre> <ul><li>Most people don’t just <a href="https://www.youtube.com/watch?v=rT-fbLFOCy0" rel="nofollow">read man pages for fun</a>. More often, the manual is used as a reference to quickly look up usage or special flags.</li> <li>You can search for text in the manual by pressing the <code>/</code> key and typing your search, then pressing enter. Let’s try to look up what the <code>-r</code> flag does for the <code>ls</code> command:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">man</span><span style="color:#99FFE4"> ls</span></span>
<span class="line"><span style="color:#8B8B8B94"># type '/-r' to start searching</span></span>
<span class="line"></span>
<span class="line"><span style="color:#8B8B8B94"># press 'n' to jump to the next result</span></span>
<span class="line"></span>
<span class="line"><span style="color:#8B8B8B94"># press 'N' to go back if you went too far</span></span></code></pre> <h3>Flags:</h3> <ul><li>some commands can take <a href="https://www.ibm.com/docs/en/aix/7.3?topic=names-command-flags" rel="nofollow">flags</a>. Flags are options that you can pass to a command to change its behavior.</li> <li>For example, the <code>ls</code> command can take a <code>-l</code> flag to show a “long” listing of files:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">ls</span><span style="color:#99FFE4"> -l</span></span></code></pre> <ul><li>The <code>ls</code> command can also take a <code>-a</code> flag to show “all” files, including hidden files:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">ls</span><span style="color:#99FFE4"> -a</span></span></code></pre> <ul><li>You can combine flags:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">ls</span><span style="color:#99FFE4"> -al</span></span></code></pre> <ul><li>Whether or not a command takes flags, and what those flags are, is up to the developer of the command. That said there are some common conventions: <ul><li>Single-character flags are prefixed with a single dash (.e.g <code>-a</code>)</li> <li>Multi-character flags are prefixed with two dashes (e.g. <code>--help</code>)</li> <li>Sometimes the same flag can be used with a single dash or two dashes (e.g. <code>-h</code> or <code>--help</code>)</li></ul></li> <li>Programming languages have functions, and functions take arguments. For example, this Python function takes two arguments: <code>xp</code> and <code>level</code>:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#A0A0A0">def</span><span style="color:#FFC799"> print_player</span><span style="color:#FFF">(xp, level):</span></span>
<span class="line"><span style="color:#FFC799">    print</span><span style="color:#FFF">(</span><span style="color:#99FFE4">"Player has"</span><span style="color:#FFF">, xp, </span><span style="color:#99FFE4">"xp and is level"</span><span style="color:#FFF">, level)</span></span></code></pre> <ul><li>In a shell, commands (programs) can also take arguments. For example, the <code>cd</code> command takes a single argument (the directory to change to):</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">cd</span><span style="color:#99FFE4"> /home/wagslane</span></span></code></pre> <ul><li>Other commands might take multiple arguments. For example, the <code>mv</code> command takes two arguments: the file to move, and the destination to move it to:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">mv</span><span style="color:#99FFE4"> file.txt</span><span style="color:#99FFE4"> newfile.txt</span></span></code></pre> <ul><li><p>By convention, most production-ready CLI tools have a “help” option that will print out some information about how to use the tool. It’s usually accessed with one of the following:</p> <ul><li><code>--help</code> (flag)</li> <li><code>-h</code> (flag)</li> <li><code>help</code> (first positional argument)</li></ul></li> <li><p>The “help” output is often easier to parse than a full <code>man</code> page. It’s usually more of a quick start guide than a full manual.</p></li></ul> <h3>Exit Codes:</h3> <ul><li><a href="https://en.wikipedia.org/wiki/Exit_status" rel="nofollow">Exit codes</a> (sometimes called “return codes” or “status codes”) are how programs communicate back whether they ran successfully or not.</li> <li><code>0</code> is the exit code for success. Any other exit code is an error. 9 times out of 10, if a non-zero exit code is returned (meaning an error) it will be <code>1</code>, which is the “catch-all” error code.</li> <li>Programs that call other programs use error codes to figure out if execution was successful. For example, if the Boot.dev server program exits with a non-zero exit code, we have another program that will automatically restart it and log the error.</li> <li>In a shell, you can access the exit code of the last program you ran with the question mark (<code>?</code>) variable. For example, if you run a program that exits with a non-zero exit code, you can see what it was with the <code>echo</code> command:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">ls</span><span style="color:#99FFE4"> ~</span></span>
<span class="line"><span style="color:#FFC799">echo</span><span style="color:#A0A0A0"> $?</span></span>
<span class="line"><span style="color:#8B8B8B94"># 0</span></span></code></pre> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">ls</span><span style="color:#99FFE4"> /does/not/exist</span></span>
<span class="line"><span style="color:#FFC799">echo</span><span style="color:#A0A0A0"> $?</span></span>
<span class="line"><span style="color:#8B8B8B94"># 1</span></span></code></pre> <h3>Standard Output:</h3> <ul><li>You might not even know it yet, but you’re already a pro at using standard output. You’ve been using it since you started the first exercise in this course.</li> <li><a href="https://en.wikipedia.org/wiki/Standard_streams#Standard_output_%28stdout%29" rel="nofollow">“Standard Output”</a>, usually called “standard out” or “stdout”, is the default place where programs print their output. It’s just a stream of data that prints to your terminal, but we’ll talk later about how it can be redirected to other places.</li> <li>All programming languages have a simple way to print to stdout. In Python, it’s the <code>print</code> function:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">print</span><span style="color:#FFF">(</span><span style="color:#99FFE4">"Hello world"</span><span style="color:#FFF">)</span></span>
<span class="line"><span style="color:#8B8B8B94"># Hello world</span></span></code></pre> <h3>Standard Error:</h3> <ul><li><a href="https://en.wikipedia.org/wiki/Standard_streams#Standard_error_%28stderr%29" rel="nofollow">“Standard Error”</a>, usually called “stderr”, is the same thing as standard output, but for error messages. It’s a stream completely separate from stdout so that you can redirect it to a different place if need be, but by default, it prints to your terminal just like stdout.</li> <li>ou can redirect stdout and stderr to different places using the <code>></code> and <code>2></code> operators. <code>></code> redirects stdout, and <code>2></code> redirects stderr.</li> <li>Redirect stdout to a file</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">echo</span><span style="color:#99FFE4"> "Hello world"</span><span style="color:#A0A0A0"> ></span><span style="color:#99FFE4"> hello.txt</span></span>
<span class="line"><span style="color:#FFC799">cat</span><span style="color:#99FFE4"> hello.txt</span></span>
<span class="line"><span style="color:#8B8B8B94"># Hello world</span></span></code></pre> <ul><li>Redirect stderr to a file</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">cat</span><span style="color:#99FFE4"> doesnotexist.txt</span><span style="color:#A0A0A0"> 2></span><span style="color:#99FFE4"> error.txt</span></span>
<span class="line"><span style="color:#FFC799">cat</span><span style="color:#99FFE4"> error.txt</span></span>
<span class="line"><span style="color:#8B8B8B94"># cat: doesnotexist.txt: No such file or directory</span></span></code></pre> <h3>Standard In:</h3> <ul><li>If there’s a standard output, there must be a standard input, right?</li> <li><a href="https://en.wikipedia.org/wiki/Standard_streams#Standard_input_%28stdin%29" rel="nofollow">“Standard Input”</a>, usually called “standard in” or “stdin”, is the default place where programs <em>read</em> their input. It’s just a stream of data that programs can read from as they run.</li> <li>All programming languages have a simple way to read from stdin. In Python, it’s the <code>input</code> function:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#8B8B8B94"># execution stops until the user types</span></span>
<span class="line"><span style="color:#8B8B8B94"># something (in this case "Lane") and presses enter</span></span>
<span class="line"><span style="color:#FFF">name </span><span style="color:#A0A0A0">=</span><span style="color:#FFC799"> input</span><span style="color:#FFF">(</span><span style="color:#99FFE4">"What is your name? "</span><span style="color:#FFF">)</span></span>
<span class="line"></span>
<span class="line"><span style="color:#FFC799">print</span><span style="color:#FFF">(</span><span style="color:#99FFE4">"Hello,"</span><span style="color:#FFF">, name)</span></span>
<span class="line"><span style="color:#8B8B8B94"># Hello, Lane!</span></span></code></pre> <h3>Piping:</h3> <ul><li>One of the most beautiful things about the shell is that you can <a href="https://en.wikipedia.org/wiki/Pipeline_%28Unix%29" rel="nofollow">pipe</a> the output of one program into the input of another program. With this one simple concept, you can run incredibly powerful automation tasks.</li> <li>The pipe operator is <code>|</code>. It’s the character that looks like a vertical line. It’s usually on the same key as the backslash (<code>\\</code>) above the enter key. The pipe operator takes the stdout of the program on the left and “pipes” it into the stdin of the program on the right.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">echo</span><span style="color:#99FFE4"> "Have you heard the tragedy of Darth Plagueis the Wise?"</span><span style="color:#A0A0A0"> |</span><span style="color:#FFC799"> wc</span><span style="color:#99FFE4"> -w</span></span>
<span class="line"><span style="color:#8B8B8B94"># 10</span></span></code></pre> <ul><li>However, instead of that text being sent to your terminal, the pipe operator pipes it into the <code>wc</code> (word count) command. The <code>wc</code> command counts the number of words in the input it receives. The <code>-w</code> flag tells <code>wc</code> to only count words.</li></ul> <h3>Interrupt:</h3> <ul><li>Sometimes a program will get stuck and you’ll want to stop it. Common reasons for this are: <ul><li>You made a typo in the command and it’s not doing what you want</li> <li>It’s trying to access the internet but you’re not connected</li> <li>It’s processing too much data and you don’t want to wait for it to finish</li> <li>There is a bug in the program causing it to hang</li></ul></li> <li>In these cases, you can stop the program by pressing <code>ctrl + c</code>. This sends a “SIGINT” signal to the program, which tells it to stop.</li></ul> <h3>Kill:</h3> <ul><li>Sometimes a program is in such a bad state (or is so malicious) that it doesn’t respond to the <code>SIGINT</code>, in which case the best option is to use another shell session (new terminal window) to manually <a href="https://www.ibm.com/docs/en/aix/7.3?topic=k-kill-command" rel="nofollow">kill</a> the program.</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">kill</span><span style="color:#99FFE4"> PID</span></span></code></pre> <ul><li><code>PID</code> stands for “process ID”. Every process that’s running on your machine has a unique ID. The <a href="https://www.ibm.com/docs/en/zos/3.1.0?topic=jobs-using-ps-command" rel="nofollow">ps</a>, “process status” command can be used to list the processes running on your machine, and their IDs:</li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">ps</span><span style="color:#99FFE4"> aux</span></span></code></pre> <ul><li>The “aux” options just mean “show all processes, including those owned by other users, and show extra information about each process”.</li></ul> <h3>Unix Philosophy:</h3> <ul><li>The <a href="https://en.wikipedia.org/wiki/Unix_philosophy" rel="nofollow">Unix Philosophy</a> is a simple set of principles that have guided the development of Unix-like operating systems for decades. It can be summarized as: <ol><li>Write programs that do one thing and do it well. <ol><li>why programs like <code>ls</code>, <code>grep</code>, and <code>less</code> exist. They do one thing, and they do it well. They don’t try to do too much.</li> <li><code>ls</code> lists files and directories</li> <li><code>grep</code> searches for text</li> <li><code>less</code> displays tex</li></ol></li> <li>Write programs to work together.</li></ol> <ul><li>Because, at least according to the Unix Philosophy, programs should do one thing and do it well, it’s easy to write programs that work together. For example, you can use <code>grep</code> to search for text in a file, and then pipe the output of <code>grep</code> into <code>less</code> to display the results interactively:</li></ul></li></ul> <pre class="shiki vesper" style="background-color:#101010;color:#FFF"><code><span class="line"><span style="color:#FFC799">grep</span><span style="color:#99FFE4"> "hello"</span><span style="color:#99FFE4"> some_file.txt</span><span style="color:#A0A0A0"> |</span><span style="color:#FFC799"> less</span></span></code></pre> <ol start="3"><li><p>Write programs to handle text streams, because that is a universal interface.
This point is more the “how” of the previous point. Programs work together easily when they all use the same interface: text streams. A text stream is just a sequence of characters that can be read or written sequentially. In other words, a text stream is just text.</p> <p>This hearkens back to the point we talked about at the beginning of this course: the shell is a command-line (text) interface. Text-based interfaces are much more powerful and extensible than graphical interfaces. That’s why developers have been using them for decades, and why what we can do with them looks like magic to the uninitiated.</p></li></ol> <h3>Package Manager:</h3> <ul><li>When you type a command like <code>apt install neovim</code>, the package manager will: <ol><li>Check to see if the package is already installed.</li> <li>If it’s not installed, it will download the package from a repository.</li> <li>It will install the package on your computer.</li> <li>It will install any dependencies that the package needs to run.</li> <li>It will (hopefully) add the package to your PATH if it should be there.</li></ol></li> <li>Good package managers keep track of what packages you have installed, and what versions of those packages you have installed. They keep your filesystem nice and tidy, making sure you haven’t installed 10 different instances of the same package or application.</li></ul>`);
      },
      $$slots: { default: true }
    }
  ]));
}
const __vite_glob_0_15 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Shells_and_Terminals_md,
  metadata
}, Symbol.toStringTag, { value: "Module" }));
export {
  __vite_glob_0_15 as _
};
